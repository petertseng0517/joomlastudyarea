# SDD：圖片裁切轉 WebP 工具

**文件版本：** 1.0  
**日期：** 2026-05-14  
**作者：** petertsengtw  
**狀態：** 草稿

---

## 1. 目的與背景

慈濟醫院同仁上傳圖片至 Joomla 前，需自行處理圖片格式與大小（限制 2MB）。目前教學文件已有警告，但缺乏輔助工具，同仁仍需另找第三方軟體。

**目標：** 在現有 Joomla 教學網站（MkDocs / GitHub Pages）內，提供一個零安裝的圖片裁切 + 轉 .webp 工具頁面，降低同仁的操作門檻。

---

## 2. 需求規格

### 2.1 功能需求

| ID | 需求描述 | 優先級 |
|----|----------|--------|
| F-01 | 使用者可拖曳或點選上傳圖片（支援 jpg / png / gif / webp） | 必要 |
| F-02 | 圖片上傳後顯示裁切介面 | 必要 |
| F-03 | 提供固定比例按鈕：`1:1`、`4:3`、`16:9`、`3:4` | 必要 |
| F-04 | 提供「自由裁切」模式（無比例限制） | 必要 |
| F-05 | 提供品質滑桿（0–100%，預設 85%） | 必要 |
| F-06 | 顯示預估輸出檔案大小（KB） | 建議 |
| F-07 | 點擊「下載」後產生 .webp 檔案並儲存到本機 | 必要 |
| F-08 | 頁面提示此工具的用途與下一步（上傳到 Joomla） | 建議 |
| F-09 | 上傳檔案 > 20MB 時顯示橘色警告，仍允許載入 | 必要 |
| F-10 | 圖片任一邊 > 4000px 時自動等比縮小後載入，使用者無感 | 必要 |
| F-11 | 輸出 .webp > 2MB 時大小顯示變紅並提示降低品質 | 必要 |
| F-12 | 提供「醫師照 (450×600)」按鈕，輸出強制縮至 450×600px | 必要 |
| F-13 | 醫師照模式下大小警告門檻改為 1MB | 必要 |
| F-14 | 醫師照模式下顯示「輸出：450×600px，建議不超過 1MB」標籤 | 建議 |

### 2.2 非功能需求

| ID | 需求描述 |
|----|----------|
| NF-01 | 純前端實作，不需後端伺服器，所有處理在瀏覽器本機完成 |
| NF-02 | 支援 Chrome / Edge（醫院 Windows 環境主流瀏覽器） |
| NF-03 | 不引入 npm / build 工具，所有依賴透過 CDN 載入 |
| NF-04 | 單一 HTML 檔案，方便維護與部署 |
| NF-05 | Safari 17+ 可用；舊版 Safari 顯示不支援提示（fallback） |

---

## 3. 系統架構

### 3.1 技術選型

| 元件 | 技術 | 說明 |
|------|------|------|
| 圖片裁切 | Cropper.js 1.6.x（CDN） | 成熟的開源裁切套件 |
| 格式轉換 | 瀏覽器原生 `HTMLCanvasElement.toBlob()` | 支援 `image/webp` |
| 樣式 | 純 CSS | 不引入框架，減少依賴 |

### 3.2 部署架構

```
GitHub Repository
└── docs/
    └── tools/
        └── image-to-webp.html   ← 工具頁面（新增）

GitHub Actions → GitHub Pages
網址：https://www.petertseng.me/hlm-pc/tools/image-to-webp.html
```

### 3.3 頁面佈局

```
┌──────────────────────────────────────────┐
│  標題：圖片裁切工具（上傳 Joomla 前使用）  │
├──────────────────────────────────────────┤
│  [拖曳圖片到此處，或點選上傳]             │
├───────────────────┬──────────────────────┤
│  原圖 + 裁切框     │  裁切後預覽           │
│  (Cropper.js)     │  (canvas)            │
├───────────────────┴──────────────────────┤
│  比例：[1:1] [4:3] [16:9] [3:4] [自由]   │
│  品質：[━━━━●━━━━━] 85%                  │
│  預估大小：約 XX KB                       │
│  [下載 .webp]                             │
├──────────────────────────────────────────┤
│  提示：下載後請至 Joomla 後台上傳圖片      │
└──────────────────────────────────────────┘
```

---

## 4. 介面規格

### 4.1 上傳區（Drop Zone）

- `id="drop-zone"`
- 接受事件：`dragover`、`drop`、`click`（觸發 `input[type=file]`）
- 接受的 MIME 類型：`image/jpeg, image/png, image/gif, image/webp`
- 上傳後隱藏 drop-zone，顯示裁切區

### 4.2 Cropper.js 初始化參數

```js
new Cropper(imageElement, {
  viewMode: 1,          // 限制裁切框在圖片範圍內
  autoCropArea: 0.8,    // 預設裁切 80% 範圍
  movable: true,
  zoomable: false,      // 關閉縮放，避免操作混亂
  aspectRatio: NaN,     // 預設自由裁切，按鈕切換
});
```

### 4.3 比例切換按鈕

| 按鈕文字 | `aspectRatio` 值 |
|----------|-----------------|
| 1:1 | `1` |
| 4:3 | `4/3` |
| 16:9 | `16/9` |
| 3:4 | `3/4` |
| 自由 | `NaN` |

### 4.4 轉換與下載邏輯

```
使用者點擊「下載」
  → cropper.getCroppedCanvas() 取得裁切後 canvas
  → canvas.toBlob(callback, 'image/webp', quality)
    → quality = 品質滑桿值 / 100
  → 建立 <a> 標籤，href = objectURL，download = 原檔名.webp
  → 觸發 click，自動下載
  → 更新預估大小顯示
```

### 4.5 預估大小顯示

- 每次品質滑桿變更時，呼叫 `canvas.toBlob()` 取得 Blob size
- 顯示格式：`約 XX KB`

---

## 5. 需修改的檔案

| 檔案路徑 | 異動類型 | 說明 |
|----------|----------|------|
| `docs/tools/image-to-webp.html` | 新增 | 工具主體頁面 |
| `mkdocs.yml` | 修改 | 在 nav 加入工具連結 |
| `docs/joomla/在joomla文章新增圖片.md` | 修改 | 頂部加入工具連結提示 |

### mkdocs.yml 修改位置

在 `基礎joomla教學` 區塊，`Joomla編輯者切勿有以下行為` 之前插入：

```yaml
- 圖片裁切轉WebP工具: tools/image-to-webp.html
```

### 在joomla文章新增圖片.md 修改

在文章開頭加入：

```markdown
> **上傳前建議先處理圖片：** [圖片裁切轉WebP工具](../tools/image-to-webp.html) — 可裁切圖片並轉成 .webp 格式，減少檔案大小。
```

---

## 6. 測試計畫

| 測試項目 | 步驟 | 預期結果 |
|----------|------|----------|
| 本機開發預覽 | `mkdocs serve`，開啟工具頁面 | 頁面正常顯示 |
| 上傳 jpg | 拖曳 jpg 到上傳區 | 顯示 Cropper 裁切介面 |
| 比例切換 | 點擊 `16:9` 按鈕 | 裁切框鎖定 16:9 |
| 自由裁切 | 點擊「自由」按鈕 | 裁切框可任意調整 |
| 品質調整 | 滑動品質滑桿至 50% | 預估大小縮小，下載檔案品質降低 |
| 下載 | 點擊「下載 .webp」 | 瀏覽器儲存 .webp 檔案，副檔名正確 |
| 檔案大小 | 上傳 2MB jpg，下載 | .webp 應明顯小於原始 jpg |
| 舊版 Safari | 在不支援 WebP output 的環境開啟 | 顯示「您的瀏覽器不支援」提示 |
